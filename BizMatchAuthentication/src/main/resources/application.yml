server:
  port: 9090
spring:
  datasource:
    password: ''
    url: jdbc:mysql://localhost:3306/BizMatchprojectMS?createDatabaseIfNotExist=true
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: smtp.gmail.com
    password: esnliwubznqsjogi
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    username: fatma.maazoun21@gmail.com

  application:
    name: BizMatchAuthentication
  config:
    import: optional:configserver:http://localhost:9999



eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/


